"use client";

const PromotionBanner = () => {
  return (
    <div className="bg-[#4B2E2E] text-[#F7D9DC] py-2 px-6 text-center text-sm font-semibold tracking-wide">
      <span className="inline-block animate-pulse">🌙</span> 
      <span className="mx-2">Eid Special Sale! Get a FREE topping with every order. Limited time only!</span>
      <span className="inline-block animate-pulse">🧋</span>
    </div>
  );
};

export default PromotionBanner;
